# Django Backend API Base URL
# Update this to match your Django server URL
VITE_API_BASE_URL=http://localhost:8000

# Optional: JWT Token Refresh Endpoint
REFRESH_ENDPOINT=/auth/token/refresh/
# If your backend uses a different endpoint, uncomment and update:
VITE_REFRESH_ENDPOINT=REFRESH_ENDPOINT
