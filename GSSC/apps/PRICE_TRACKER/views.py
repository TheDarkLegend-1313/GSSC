from django.shortcuts import render

# Create your views here.
def price_tracker_view(request):
    # Placeholder for processing click events
    return render(request, 'dashboard/price_tracker.html') 
